import { SiteHeader,SiteFooter } from '@/components/site-header';
import ModelLaunch from '@/components/model-launch';
import {modelIds} from '@/lib/model-catalog';
export const metadata={title:'3D Collection — Omar Bacho'};
export default function Models(){return <main id="top"><SiteHeader/><section id="main-content" className="collection-page-title"><p className="eyebrow">{modelIds.length} interactive studies</p><h1>In three dimensions.</h1><p>Select a project, then explore its components, assembly and spatial details. Every model includes touch, mouse and optional hand-gesture controls.</p><p className="models-qualification">Two selected reconstructions, with the original reference images inside each viewer. Approximate proportions.</p></section><section className="model-library">{modelIds.map(id=><ModelLaunch key={id} model={id}/>)}</section><section className="source-drawings"><p className="eyebrow">From the portfolios</p><h2>Original spatial drawings</h2><p>Explore the original plans, perspectives and axonometrics. These are portfolio images, not interactive model files.</p><div className="source-drawing-links">{[["landship","Landship"],["artists-studio-balcony","Artist’s Studio Balcony"],["lebanese-concept-house","Lebanese Concept House"],["residence-du-parc","Résidence du Parc"]].map(([id,title])=><a key={id} href={"/work/"+id+"#drawings"}>{title} ↗</a>)}</div></section><SiteFooter/></main>}
