'use client';
import { useEffect, useRef, useState } from 'react';
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { createBenina,createThing,createLandship,createStudio,createTwig,createParc,disposeObject,type StudyPart } from '@/lib/spatial-models';
import { modelCatalog,type ModelId } from '@/lib/model-catalog';
import { Slider } from '@/components/ui/slider';
import { Switch } from '@/components/ui/switch';
import type { HandLandmarker } from '@mediapipe/tasks-vision';
type ViewerAPI={rotate:(x:number,y:number)=>void;zoom:(factor:number)=>void;reset:()=>void;preset:(name:'perspective'|'plan'|'front')=>void;select:(id:string|null)=>void;focus:()=>void};
export default function ModelViewer({model}:{model:ModelId}){
 const config=modelCatalog[model];
 const [parts,setParts]=useState<StudyPart[]>([]),[selected,setSelected]=useState<string|null>(null),[detail,setDetail]=useState(0),[gesturesOpen,setGesturesOpen]=useState(false),[view,setView]=useState<'perspective'|'plan'|'front'>('perspective');
 const mount=useRef<HTMLDivElement>(null),video=useRef<HTMLVideoElement>(null),stream=useRef<MediaStream|null>(null),tracker=useRef<HandLandmarker|null>(null),raf=useRef(0),alive=useRef(true),requestId=useRef(0),api=useRef<ViewerAPI|null>(null);
 const[explode,setExplode]=useState(0),[field,setField]=useState(true),[roof,setRoof]=useState(true),[camera,setCamera]=useState<'off'|'loading'|'on'>('off'),[status,setStatus]=useState(''),[renderError,setRenderError]=useState('');
 const settings=useRef({explode,roof,detail});settings.current={explode,roof,detail};
 function stopCamera(){requestId.current++;cancelAnimationFrame(raf.current);stream.current?.getTracks().forEach(t=>t.stop());stream.current=null;tracker.current?.close();tracker.current=null;if(video.current)video.current.srcObject=null;if(alive.current){setCamera('off');setStatus('')}}
 useEffect(()=>{alive.current=true;const hidden=()=>{if(document.hidden)stopCamera()};document.addEventListener('visibilitychange',hidden);return()=>{alive.current=false;document.removeEventListener('visibilitychange',hidden);stopCamera()}},[]);
 useEffect(()=>{
  const host=mount.current;if(!host)return;let renderer:THREE.WebGLRenderer;
  try{renderer=new THREE.WebGLRenderer({antialias:true,alpha:false})}catch{setRenderError('3D is unavailable in this browser. The project drawings are available below.');return}
  setRenderError('');renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.setClearColor('#edf0e9');renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.domElement.setAttribute('aria-label',`${config.title} 3D study. Drag to orbit, scroll to zoom. Select a component or use the labeled controls.`);renderer.domElement.setAttribute('role','img');host.appendChild(renderer.domElement);
  const scene=new THREE.Scene();const camera3=new THREE.PerspectiveCamera(35,1,.1,250);
  const study=model==='benina'?createBenina(field):model==='thing'?createThing():model==='landship'?createLandship():model==='studio'?createStudio():model==='twig'?createTwig():createParc();
  scene.add(study.root);study.root.updateMatrixWorld(true);setParts(study.parts);setSelected(null);setView('perspective');
  const box3=new THREE.Box3().setFromObject(study.root),size=box3.getSize(new THREE.Vector3()),target=box3.getCenter(new THREE.Vector3());
  const distance=Math.max(size.x,size.y,size.z)*1.75+2;const initial=target.clone().add(new THREE.Vector3(.85,.65,1).normalize().multiplyScalar(distance));camera3.position.copy(initial);
  const controls=new OrbitControls(camera3,renderer.domElement);controls.target.copy(target);controls.enableDamping=true;controls.dampingFactor=.075;controls.minDistance=2;controls.maxDistance=distance*3;controls.maxPolarAngle=Math.PI*.49;controls.enablePan=true;
scene.add(new THREE.HemisphereLight('#ffffff','#b0ba9e',2.3));const sun=new THREE.DirectionalLight('#ffefd8',3);sun.position.set(10,17,8);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);Object.assign(sun.shadow.camera,{left:-15,right:15,top:15,bottom:-15});sun.shadow.bias=-.0003;scene.add(sun);
  const floor=new THREE.Mesh(new THREE.PlaneGeometry(100,100),new THREE.MeshStandardMaterial({color:'#e4e9dd',roughness:1}));floor.rotation.x=-Math.PI/2;floor.position.y=-.16;floor.receiveShadow=true;scene.add(floor);const grid=new THREE.GridHelper(36,36,'#c9d1c1','#d6ddcf');grid.position.y=-.145;scene.add(grid);
  const resize=()=>{const{width,height}=host.getBoundingClientRect();renderer.setSize(width,height);camera3.aspect=width/Math.max(height,1);camera3.updateProjectionMatrix()};const observer=new ResizeObserver(resize);observer.observe(host);resize();
  let selectedId:string|null=null;const highlight=new THREE.MeshStandardMaterial({color:'#c69a4e',roughness:.7});
  function selectPart(id:string|null){if(id&&study.parts.find(p=>p.id===id)?.node.visible===false)setRoof(true);selectedId=id;setSelected(id);study.root.traverse(o=>{if(o instanceof THREE.Mesh){o.userData.originalMaterial??=o.material;let p:THREE.Object3D|null=o,hit=false;while(p){if(p.userData.partId===id&&id)hit=true;p=p.parent}o.material=hit?highlight:o.userData.originalMaterial}})}
  function reset(){camera3.position.copy(initial);controls.target.copy(target);controls.update();setView('perspective')}
  api.current={rotate(x,y){const offset=camera3.position.clone().sub(controls.target);const s=new THREE.Spherical().setFromVector3(offset);s.theta+=x;s.phi=THREE.MathUtils.clamp(s.phi+y,.02,Math.PI*.48);camera3.position.copy(controls.target).add(new THREE.Vector3().setFromSpherical(s));controls.update()},zoom(f){const d=camera3.position.clone().sub(controls.target);d.setLength(THREE.MathUtils.clamp(d.length()*f,controls.minDistance,controls.maxDistance));camera3.position.copy(controls.target).add(d);controls.update()},reset,preset(name){controls.target.copy(target);camera3.position.copy(target).add(name==='plan'?new THREE.Vector3(0,distance,.01):name==='front'?new THREE.Vector3(0,target.y*.02,distance):initial.clone().sub(target));controls.update();setView(name)},select:selectPart,focus(){const p=study.parts.find(p=>p.id===selectedId);if(!p)return;study.root.updateMatrixWorld(true);const b=new THREE.Box3().setFromObject(p.node),c=b.getCenter(new THREE.Vector3()),r=b.getSize(new THREE.Vector3()).length()*.85;const offset=camera3.position.clone().sub(controls.target).normalize().multiplyScalar(Math.max(r,2));controls.target.copy(c);camera3.position.copy(c).add(offset);controls.update()}};
  const raycaster=new THREE.Raycaster(),pointer=new THREE.Vector2();let down={x:0,y:0};const onDown=(e:PointerEvent)=>{down={x:e.clientX,y:e.clientY}};const onUp=(e:PointerEvent)=>{if(Math.hypot(e.clientX-down.x,e.clientY-down.y)>5)return;const rect=renderer.domElement.getBoundingClientRect();pointer.set((e.clientX-rect.left)/rect.width*2-1,-(e.clientY-rect.top)/rect.height*2+1);raycaster.setFromCamera(pointer,camera3);const hits=raycaster.intersectObject(study.root,true).filter(h=>{let a:THREE.Object3D|null=h.object;while(a){if(!a.visible)return false;a=a.parent}return true});let p:THREE.Object3D|null=hits[0]?.object||null;while(p&&!p.userData.partId)p=p.parent;selectPart(p?.userData.partId||null)};
  renderer.domElement.addEventListener('pointerdown',onDown);renderer.domElement.addEventListener('pointerup',onUp);
  let frame=0,currentExplode=0;function animate(){currentExplode=THREE.MathUtils.lerp(currentExplode,settings.current.explode/100,.1);study.setExplode(currentExplode);study.setRoof(settings.current.roof);study.setDetail?.(settings.current.detail);controls.update();renderer.render(scene,camera3);frame=requestAnimationFrame(animate)}animate();
  return()=>{cancelAnimationFrame(frame);observer.disconnect();renderer.domElement.removeEventListener('pointerdown',onDown);renderer.domElement.removeEventListener('pointerup',onUp);study.root.traverse(o=>{if(o instanceof THREE.Mesh&&o.userData.originalMaterial)o.material=o.userData.originalMaterial});highlight.dispose();controls.dispose();disposeObject(scene);renderer.dispose();renderer.forceContextLoss();renderer.domElement.remove();api.current=null}
 },[model,field]);
 async function startCamera(){
  if(!navigator.mediaDevices?.getUserMedia){setStatus('Camera gestures need a browser with camera access. Drag and zoom controls are ready to use.');return}
  const id=++requestId.current;setCamera('loading');setStatus('Preparing hand tracking…');
  try{
   const{FilesetResolver,HandLandmarker}=await import('@mediapipe/tasks-vision');const files=await FilesetResolver.forVisionTasks('/hand-tracking/wasm');const hand=await HandLandmarker.createFromOptions(files,{baseOptions:{modelAssetPath:'/hand-tracking/hand_landmarker.task',delegate:'CPU'},runningMode:'VIDEO',numHands:2,minHandDetectionConfidence:.65,minTrackingConfidence:.6});
   if(!alive.current||id!==requestId.current){hand.close();return}tracker.current=hand;setStatus('Allow camera access to use hand gestures.');
   const media=await navigator.mediaDevices.getUserMedia({video:{facingMode:'user',width:640,height:480},audio:false});
   if(!alive.current||id!==requestId.current){media.getTracks().forEach(t=>t.stop());return}stream.current=media;const v=video.current;if(!v)throw new Error('Camera preview unavailable');v.srcObject=media;await v.play();if(!alive.current||id!==requestId.current)return;setCamera('on');setStatus('Show your hand to the camera.');
   let lastTime=-1,lastFrame=0,lastPoint:{x:number;y:number}|null=null,lastDistance:number|null=null,lastMessage='';
   const message=(s:string)=>{if(s!==lastMessage){lastMessage=s;setStatus(s)}};
   const loop=(now:number)=>{
    if(!alive.current||id!==requestId.current||!tracker.current)return;
    try{if(v.readyState>=2&&now-lastFrame>85&&v.currentTime!==lastTime){lastFrame=now;lastTime=v.currentTime;const hands=tracker.current.detectForVideo(v,now).landmarks;
     if(hands.length>=2){const a=hands[0][9],b=hands[1][9];const dist=Math.hypot(a.x-b.x,a.y-b.y);if(lastDistance&&dist>.06)api.current?.zoom(THREE.MathUtils.clamp(lastDistance/dist,.92,1.08));lastDistance=dist;lastPoint=null;message('Two hands detected · Move apart to zoom in.')}else if(hands.length===1){lastDistance=null;const h=hands[0];const palm=Math.hypot(h[0].x-h[9].x,h[0].y-h[9].y);const pinch=Math.hypot(h[4].x-h[8].x,h[4].y-h[8].y)<palm*.45;if(pinch){const point={x:h[8].x,y:h[8].y};if(lastPoint)api.current?.rotate((point.x-lastPoint.x)*-5,(point.y-lastPoint.y)*-3);lastPoint=point;message('Pinch held · Move your hand to rotate.')}else{lastPoint=null;message('Hand detected · Pinch thumb and index finger to rotate.')}}else{lastPoint=null;lastDistance=null;message('Looking for your hands…')}}
     raf.current=requestAnimationFrame(loop)
    }catch{stopCamera();setStatus('Hand tracking stopped. Try again, or use drag and zoom controls.')}
   };raf.current=requestAnimationFrame(loop);
  }catch(error){if(id!==requestId.current)return;stopCamera();if(alive.current)setStatus(error instanceof DOMException&&error.name==='NotAllowedError'?'Camera access was declined. You can still drag, rotate and zoom.':'Camera gestures could not start. Check camera availability, then try again.');}
 }
 const activePart=parts.find(p=>p.id===selected);
 return <div className="viewer viewer-detailed"><div className="viewer-main"><div className="viewer-stage"><div className="three-mount" ref={mount}/>{renderError&&<div className="render-error">{renderError}</div>}<div className="view-label"><span>{config.subtitle}</span><small>Approximate proportions · Source-based study</small></div><div className="view-instructions">Drag to rotate · Select a component to learn more</div><div className={'camera-preview '+(camera==='on'?'visible':'')}><video ref={video} muted playsInline autoPlay/><span>Camera on · Local processing</span></div></div><div className="view-bar"><div className="view-presets" role="group" aria-label="Model view">{(['perspective','plan','front'] as const).map(v=><button key={v} aria-pressed={view===v} onClick={()=>api.current?.preset(v)}>{v==='perspective'?'Perspective':v==='plan'?'Top view':'Front view'}</button>)}</div><div className="view-actions"><button onClick={()=>api.current?.zoom(.85)} aria-label="Zoom in">+</button><button onClick={()=>api.current?.zoom(1.18)} aria-label="Zoom out">−</button><button onClick={()=>{api.current?.reset();setExplode(0);setRoof(true);setDetail(0);api.current?.select(null)}}>Reset view</button></div></div></div><aside className="model-inspector"><h3>Explore the model</h3><p>{config.explore}</p><div className="model-part-list" role="group" aria-label="Model components">{parts.map((p,i)=><button key={p.id} aria-pressed={selected===p.id} onClick={()=>api.current?.select(selected===p.id?null:p.id)}><span>{String(i+1).padStart(2,'0')}</span>{p.label}<b>{selected===p.id?'−':'+'}</b></button>)}</div>{activePart&&<div className="component-detail" role="status"><h4>{activePart.label}</h4><p>{activePart.detail}</p><button onClick={()=>api.current?.focus()}>Focus on this part ↗</button></div>}<div className="inspector-options">{model==='benina'&&<label><Switch checked={field} onCheckedChange={setField}/>Show multiple bays</label>}<label><Switch checked={roof} onCheckedChange={setRoof}/>Show {config.layer.toLowerCase()}</label><div className="explode-control"><label htmlFor="explode-slider">Separate components <span>{explode}%</span></label><Slider id="explode-slider" aria-label="Separate model components" value={[explode]} onValueChange={v=>setExplode(Array.isArray(v)?v[0]:v)} min={0} max={100} step={1}/></div>{config.action&&<div className="explode-control"><label htmlFor="detail-slider">{config.action}<span>{detail}{config.unit}</span></label><Slider id="detail-slider" aria-label={config.action} value={[detail]} onValueChange={v=>setDetail(Array.isArray(v)?v[0]:v)} min={0} max={config.max} step={1}/></div>}</div></aside><div className="gesture-disclosure"><button className="gesture-disclosure-trigger" aria-expanded={gesturesOpen} onClick={()=>{setGesturesOpen(!gesturesOpen);if(gesturesOpen)stopCamera()}}>Use hand gestures <span>{camera==='on'?'Camera on':gesturesOpen?'−':'Optional camera control +'}</span></button>{gesturesOpen&&<div className="gesture-panel"><div><ol className="gesture-steps"><li>Position one or two hands in front of the camera.</li><li>Pinch thumb and index finger; move to rotate.</li><li>Move two hands apart to zoom in.</li></ol><p className="camera-privacy">Video is processed on your device. It is not recorded or uploaded.</p></div><button className="camera-button" disabled={!!renderError} onClick={camera==='off'?startCamera:stopCamera}>{camera==='off'?'Enable camera':camera==='loading'?'Cancel camera setup':'Turn camera off'}</button>{status&&<p className="gesture-status" role="status">{status}</p>}</div>}</div><div className="model-source-note">{config.source}. Interpretive geometry; not a surveyed or fabrication model.</div></div>
}
